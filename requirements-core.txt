# Зависимости ядра анализа. Ставятся в core-venv (см. DEPLOY.md).
#
# pypdf нужен для чтения .pdf — одного из трёх обязательных форматов.
# Без него ядро отвечает на PDF ошибкой «нет пакета pypdf», а не разбирает
# документ, поэтому пакет не опционален.
requests
pyyaml
pypdf>=6.0
